// Arrays are objects
// Arrays have some in-build properties and methods i.e push and pop
// push is used to add items to an array
// pop is used to remove items from an array

// 1. how to create an array;
  //var laptop=['size','memory'];

// adding items to an array (push());;
 var fruits=[];
 fruits.push('mango');
 fruits.push('melon')
 console.log(fruits)

 //removing items from an array pop

  fruits.pop();
  console.log(fruits);
   

  var clothes=[];
  clothes.push('tops','leggings', 'socks', 'trousers', 'sweaters');
  clothes.pop();
  clothes.push('shoes');
  console.log(clothes);
